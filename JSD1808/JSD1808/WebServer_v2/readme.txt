readme.txt
在core包中创建ClientHandler,这是一个线程的任务用于处理与一个客户端的交互工作

在WebServer中当接收了一个客户端连接后就启动了一个线程来与该客户端交互

以上操作与聊天室结构一致。


由于请求中，请求行与消息头都是一行一行的字符串（CRLF结尾），
为了便于解析这些消息，我们定义下面的方法：
在ClientHandler中实现一个方法，测试可以一次性读取一行客户端发送的字符串操作：readLine方法

